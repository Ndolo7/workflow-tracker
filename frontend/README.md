# Workflow Tracker Frontend

Vite + React frontend for the workflow tracker.

## Setup

```bash
npm install
npm run dev
```

## Environment

Copy `.env.example` to `.env` and update values as needed.
